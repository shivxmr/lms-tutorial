/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "www.google.com", "plus.unsplash.com", "unsplash.com"],
  },
};

export default nextConfig;
